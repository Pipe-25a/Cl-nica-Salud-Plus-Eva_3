# Clínica Salud Plus — Agente RAG con Memoria Dual
# Dependencias del proyecto

# Core LangChain
langchain>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0
langchain-text-splitters>=0.2.0

# Agente ReAct (LangGraph) — usado en src/agent_core.py
# (create_react_agent, MemorySaver). Faltaba en la lista original.
langgraph>=0.2.0

# LLM y Embeddings
openai>=1.30.0

# Base de datos vectorial
chromadb>=0.5.0

# Memoria y persistencia
langchain-core>=0.2.0

# Variables de entorno
python-dotenv>=1.0.0

# Utilidades
pypdf>=4.0.0